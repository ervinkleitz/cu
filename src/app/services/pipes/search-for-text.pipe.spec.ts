import { SearchForTextPipe } from './search-for-text.pipe';

describe('SearchForTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchForTextPipe();
    expect(pipe).toBeTruthy();
  });
});
